Flask==3.0.0
openai==1.3.5
requests==2.31.0
numpy==1.26.4
pandas==2.2.0
sqlite3  # (built-in with Python, but can be mentioned for clarity)
beautifulsoup4==4.12.2  # For web scraping (if needed)
lxml==5.1.0  # For parsing HTML/XML (if needed)
tqdm==4.66.1  # For progress bars (optional)
Faker==19.6.2
python-dotenv
